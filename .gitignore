# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cache incremental do `tsc -b` (npm run typecheck)
*.tsbuildinfo

.wrangler/

# Claude Code local settings (contain API tokens)
.claude/

# Lockfile — let Cloudflare Pages generate for Linux
package-lock.json

# Lockfile do Deno gerado na raiz por engano (a config Deno vive em
# supabase/functions/deno.json, nada no build o le)
/deno.lock

# Environment variables
.env
.env.local
.env.production

# Local-only business docs (budget/strategy drafts, not for the public repo)
GOOGLE-ADS-PORTO-CAMPANHA.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.tmp-*.mjs

# Google Ads strategy docs (budget/keywords, not for the public repo)
g-ads/

# Ad-hoc CRM data scripts (customer addresses + commission figures, not for the public repo)
/supabase/*.sql

# Estado local da CLI do Supabase (por máquina)
supabase/.temp/

# Exportações de conversas/serviços com valores por trabalho: nunca no repositório público.
scripts/*-raw.txt
